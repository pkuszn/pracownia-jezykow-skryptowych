class Product
    def initialize(category, producer, name, price, link)
       @category = category
       @producer = producer
       @name = name
       @price = price
       @link = link
    end
 end