env LAPIS_ENVIRONMENT=development;
http {
    # Other http block configurations go here

    server {
        listen 80;
        server_name example.com;

        location / {
            proxy_pass http://127.0.0.1:8080;
            # Additional proxy configurations go here
        }
    }
}
